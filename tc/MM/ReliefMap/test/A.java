import java.util.*;import java.awt.*;import java.awt.image.*;import java.io.*;import java.security.*;import javax.swing.*;import javax.imageio.*;    public class A {         public static void main(String [] args)        {            try {                String seed="3";                SecureRandom r = SecureRandom.getInstance("SHA1PRNG");                r.setSeed(Long.parseLong(seed));                int H = r.nextInt(451)+50;                int W = r.nextInt(451)+50;                System.out.println("W = "+W+"\nH = "+H);                //generate the elevation map                int Nextr = r.nextInt(11)+15;                int[] x, y, z, f;                int i, j, k, l;                double[] w;                double n, d, dist;                x = new int[Nextr];                y = new int[Nextr];                z = new int[Nextr];                f = new int[Nextr];                w = new double[Nextr];                for (k=0; k<Nextr; k++)                {   x[k] = r.nextInt(W);                    y[k] = r.nextInt(H);                    z[k] = r.nextInt(100);                    f[k] = r.nextInt(100)+100;                    w[k] = r.nextDouble()*0.5+0.3;                System.out.println(                        "x="+x[k]+", "+                        "y="+y[k]+", "+                        "z="+z[k]+", "+                        "f="+f[k]+", "+                        "w="+w[k]+", ");                }            }catch (Exception e) {                 System.err.println("An exception occured while generating the test case.");                e.printStackTrace();             }        }    }