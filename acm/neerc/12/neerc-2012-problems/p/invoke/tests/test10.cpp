/*
 * Invoke for NT test.
 * Redirection test
 *   outcome = success
 */
#include <iostream>
using namespace std;

int main() {
    int x, y;
    cin >> x >> y;
    cout << (x + y) << endl;
    return 0;
}
